import React from 'react';
import { Volume2, Gamepad2, Car, Monitor, Settings } from 'lucide-react';
const a = (
  <div>
                {/* SETTINGS PANEL */}
                {true && (
                  <div style={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
                  </div>
                )}
  </div>
);
